{
  "files_changed": [
    ".cortex/plans/2026-09-14-backup-controller/library-contract-findings.md",
    "internal/api/v1alpha1/groupversion_info.go",
    "internal/api/v1alpha1/volumerestore_types.go",
    "internal/api/v1alpha1/conditions.go",
    "internal/api/v1alpha1/crd_validation_test.go",
    "internal/api/v1alpha1/zz_generated.deepcopy.go",
    "config/crd/backup.wlz.li_volumerestores.yaml",
    "config/samples/backup.wlz.li_v1alpha1_volumerestore.yaml",
    "config/samples/claim.yaml",
    ".github/workflows/ci.yaml",
    "go.mod",
    "go.sum"
  ],
  "diffs_per_worker": {
    "impl-t0-contract": "One repository file: .cortex/plans/2026-09-14-backup-controller/library-contract-findings.md, 15787 bytes, plus scratch under .tmp/contract/ (a contract module whose go.mod carries github.com/kubernetes-csi/lib-volume-populator/v3 v3.3.0 and github.com/backube/volsync v0.16.0). Nothing else in the tree is task 0's: flake.nix, flake.lock, .envrc, .gitignore, Makefile, README.md, docs/* and .cortex/plans/* carry no task-0 edit, and the root go.mod carries no lib-volume-populator dependency. The document states the pin, eleven verdict rows (nine CONFIRMED, two DIFFERENT), the per-DIFFERENT consequences, and the two identifier lists for tasks 3 and 4; meta-w2 resolved every citation first-hand in the module cache and all eleven hold.",
    "impl-t2-api": "11 files, 1329 insertions, staged (git diff --cached --stat over config, internal, go.mod, go.sum, .github/workflows/ci.yaml). Per file: .github/workflows/ci.yaml 106, config/crd/backup.wlz.li_volumerestores.yaml 431, config/samples/backup.wlz.li_v1alpha1_volumerestore.yaml 11, config/samples/claim.yaml 18, go.mod 58, go.sum 161, internal/api/v1alpha1/conditions.go 38, crd_validation_test.go 195, groupversion_info.go 28, volumerestore_types.go 133, zz_generated.deepcopy.go 150. Makefile, flake.nix and ci.yaml's check job are untouched. Deviation carried: MoverPodLabels is map[string]MoverPodLabelValue (MaxLength=63) with MaxProperties=8, plus the VolumeRestoreSpec.MoverLabels() helper, with both CEL rule texts unchanged and the JSON shape unchanged."
  },
  "gate_evidence": "/home/nixos/repos/backup-controller/.cortex/reports/handbacks/w2-gate-evidence.txt",
  "whole_change_output": "Wave 2 (dispatch 2026-09-14-bc-w2), workers impl-t0-contract (task 0) and impl-t2-api (task 2), meta meta-w2. VERDICT: both tasks meet their specs, the wave's whole-change checks are green on the frozen artifact, and one defect meta-w2 found in review was corrected by its owner and re-proven. No commits; the work sits staged in the working tree (35 staged files: wave 1's 26 plus this wave's nine new files). CHECKS RUN BY META-W2 ON THE FINAL ARTIFACT: go vet ./... exit 0; go build ./... exit 0; go test ./... -race exit 0 (internal/api/v1alpha1 reports no test files, which is the build tag doing its job, not a skip); golangci-lint run 0 issues, exit 0; make verify regenerates into .tmp/crd with diff -ru printing nothing, exit 0; actionlint silent, exit 0; kubectl-validate on config/crd/backup.wlz.li_volumerestores.yaml prints OK, exit 0; the tagged envtest suite with -count=1 passes all six CRD rows (valid created and stored in 2.01s, five malformed objects each refused with apierrors.IsInvalid) plus the read-back round trip, exit 0. Raw logs: .cortex/reports/handbacks/w2-whole-change.log and w2-envtest-meta.log. TASK 0: the findings document exists and states the pin task 3 must use, github.com/kubernetes-csi/lib-volume-populator/v3 v3.3.0 (the /v3 suffix is why the un-suffixed query stops at v1.2.0). All eleven questions carry a verdict. Meta-w2 read every cited location in the module cache and each one holds: RunControllerWithConfig and VolumePopulatorConfig's fields, the pod and provider modes being mutually exclusive, the callback signatures and the requeue-without-error path for PopulateCompleteFn returning false, prime-<uid> with the copied claim fields and volume.kubernetes.io/selected-node, the data source fetch into PopulatorParams.Unstructured, the PV claimRef patch and populated-from annotation before cleanup, go 1.26.0 with k8s.io v0.36.0, no health route beside the metrics path (DIFFERENT h), no image in provider-function mode (DIFFERENT i), and VolSync's empty-repository success plus status.latestMoverStatus.result Failed on a failed mover. Both DIFFERENT findings match how the wave-3 and wave-4 specs already consume them, so neither needed escalating; one wording nit is recorded (finding i's preamble says the design requires an image flag while task 4's text defers the question to this finding). TASK 2: the API types, the generated CRD, the samples and the two appended CI jobs match the spec's tables and markers, and the generated CRD carries maxProperties 8, additionalProperties maxLength 63 and both label rules verbatim. REPAIR: the MoverPodLabels description still said at most 16 against the landed bound of 8, and that text ships inside the CRD that task 5 copies into deploy/ and chart/, so meta-w2 sent it back to the owning worker, which corrected it to 8, regenerated, re-ran make verify (no diff) and re-ran the tagged suite with -count=1 after noticing Go's test cache does not track the CRD YAML. OPEN ITEM FOR THE ORCHESTRATOR: task 3's spec tells the worker to assign the mover labels field directly, but the field is map[string]MoverPodLabelValue, so the spec text should name VolumeRestoreSpec.MoverLabels() map[string]string, which exists for exactly that assignment. The deviation behind it is measured, not stylistic: at 8 entries with unbounded string values the CEL rule is still 3.0x over the API server's cost budget, so neither bound reduction nor rule shortening rescues the unbounded form; the value-length bound is what makes the mandated rules install, with the JSON shape and both rule texts unchanged. HOLDS: none outstanding. The CRD cost-budget escalation was blocked for a while by an Ask dialog in the orchestrator's session that was addressed to the user, and meta-w2 answered nothing on the user's behalf and pushed nothing through another channel; the user answered, the orchestrator accepted the finding as its own spec defect, and meta-w2 relayed the corrected spec text to the worker verbatim, which consumed it. Both workers settled before this hand-back; no worker reads in flight."
  ,
  "deliverable_paths": [
    "/home/nixos/repos/backup-controller/.cortex/plans/2026-09-14-backup-controller/library-contract-findings.md"
  ]
}
